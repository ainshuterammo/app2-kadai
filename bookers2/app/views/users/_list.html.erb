<% books.each do |book| %>


<div>

  <%= image_tag @user.get_profile_image(100,100) %>
</div>




<div>
  <h2>User info</h2>
  <%= link_to book_path(book.id) do %>
    <%= image_tag book.get_image %>
  <% end %>
  <p>ユーザー画像：<%= image_tag book.user.get_profile_image(100,100) %></p>
  <h3><%= @user.name %></h3>
  <p>ユーザーネーム：<%= book.user.name %></p>
  <p><%= link_to "Edit", edit_book_path(book.id) %></p>
  <p>introduction：<%= book.caption %></p>
</div>


<h2>New book</h2>
<%= form_with model: @book, url: 'books', method: :post do |f| %>

<% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

  <div class="field">
    <label>Title</label>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <label>Body</label>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit 'Create Book' %>
  </div>


<% end %>

<div class='row'>
  <a class="btn btn-outline-secondary btn-block edit_user_18402" href="/users/18402/edit">
    <i class="fas fa-user-cog"></i>
</a>
</div>
      <h2 class="mt-3">New book</h2>
      <form action="/books" accept-charset="UTF-8" method="post"><input type="hidden" name="authenticity_token" value="jTI7UX30gflZawl_oo57HDqLwV36YugsWyP5AT295Gl70EkZ8DXK8TH7r2Rg56MOmmDIePZt-eFXB6mEnmoXaw" autocomplete="off" />
  <div class="form-group">
    <label for="book_title">Title</label>
    <input class="form-control book_title" type="text" name="book[title]" id="book_title" />
  </div>
  <div class="form-group">
    <label for="book_opinion">Opinion</label>
    <textarea class="form-control book_body" name="book[body]" id="book_body">
</textarea>
  </div>
  <div class="form-group">
    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book" />
  </div>